import javax.swing.JOptionPane;
import javax.swing.JFrame;

class GUItutorialClass{
    public static void main(String[] args){
        /*
        //This section is a calculator program that adds two numbers together

        //showInputDialog will provide a popup window with the parameter as a Prompts
        //it will then take input
        String fn = JOptionPane.showInputDialog("Enter first number ");
        String sn = JOptionPane.showInputDialog("Enter second number");

        //Interger.parseInt converts a variable to an integer
        int num1 = Integer.parseInt(fn);
        int num2 = Integer.parseInt(sn);
        int sum = num1 + num2;

        //showMessageDialog shows a message as a popup window
        JOptionPane.showMessageDialog(null, "The answer is " + sum, "Answer", JOptionPane.PLAIN_MESSAGE);
        */

        /*
        //Create a new window object
        basicWindow seansWindow = new basicWindow();

        //Make sure it closes when the user presses x
        seansWindow.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        //set the pixel size to 300 by 200
        seansWindow.setSize(300, 200);

        //Set the visibility to true
        seansWindow.setVisible(true);
        */

        /*
        eventWindow seansWindow = new eventWindow();
        seansWindow.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        seansWindow.setSize(300,200);
        seansWindow.setVisible(true);
        */

        //This class demonstrates how to create a button out of your own personal image
        /*
        iconButton seansWindow = new iconButton();
        seansWindow.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        seansWindow.setSize(1920,1080);
        seansWindow.setVisible(true);
        */

        //This demonstrates the use of fonts and check boxes
        /*
        checkBoxes seansWindow = new checkBoxes();
        seansWindow.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        seansWindow.setSize(500,500);
        seansWindow.setVisible(true);
        */

    }
}
